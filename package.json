{
  "name": "LocalMode",
  "version": "2.3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/LocalMode-AI/LocalMode.git"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@eslint/js": "^9.39.2",
    "@types/node": "^24",
    "eslint": "^9",
    "eslint-plugin-react-hooks": "^7.0.1",
    "globals": "^14.0.0",
    "jsdom": "^29.0.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.51.0",
    "vite": "^7.3.1",
    "vitest": "^4.1.0"
  },
  "description": "Local-first AI toolkit for the browser",
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:types": "tsc --noEmit -p packages/core/tsconfig.typetest.json",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm run build && changeset publish",
    "size": "pnpm run size --workspace=@localmode/core",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "lint:fix": "eslint . --fix",
    "check:peers": "node scripts/check-peer-ranges.mjs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
