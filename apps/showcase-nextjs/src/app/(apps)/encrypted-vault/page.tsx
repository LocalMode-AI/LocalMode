/**
 * @file page.tsx
 * @description Entry point for the Encrypted Vault application
 */
'use client';

import { VaultView } from './_components/vault-view';

/** Encrypted Vault page component */
export default function EncryptedVaultPage() {
  return <VaultView />;
}
