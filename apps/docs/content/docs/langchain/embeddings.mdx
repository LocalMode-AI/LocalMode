---
title: 'Embeddings'
description: LocalModeEmbeddings — use any LocalMode embedding model with LangChain.
icon: Cpu
---

import { Callout } from 'fumadocs-ui/components/callout';

# LocalModeEmbeddings

Drop-in replacement for `OpenAIEmbeddings` or any LangChain `Embeddings` class, backed by local inference.

<Callout type="info" title="See it in action">
  Switch the [Knowledge Base block](https://localmode.ai/blocks/knowledge/semantic-search) to its LangChain engine — document ingest and search there run through `LocalModeEmbeddings`, live in the browser.
</Callout>

## Constructor

```typescript
import { LocalModeEmbeddings } from '@localmode/langchain';
import { transformers } from '@localmode/transformers';

const embeddings = new LocalModeEmbeddings({
  model: transformers.embedding('Xenova/bge-small-en-v1.5'),
});
```

| Option | Type | Required | Description |
|--------|------|----------|-------------|
| `model` | `EmbeddingModel` | Yes | Any `@localmode/core` `EmbeddingModel` instance |

The constructor also accepts an optional second parameter of LangChain `EmbeddingsParams` for callback configuration.

## Methods

### embedDocuments

Embed multiple texts. Returns `number[][]` (LangChain format).

```typescript
const vectors = await embeddings.embedDocuments(['Hello world', 'How are you?']);
// vectors: number[][] — each inner array has 384 dimensions
```

Internally calls `model.doEmbed({ values: texts })` and converts each `Float32Array` to `number[]` via `Array.from()`. Returns `[]` for empty input without calling the model.

### embedQuery

Embed a single query text. Returns `number[]`.

```typescript
const queryVector = await embeddings.embedQuery('search term');
// queryVector: number[] — 384 dimensions
```

## Supported Models

Any `EmbeddingModel` from `@localmode/transformers`:

| Model | Dimensions | Size | Best For |
|-------|-----------|------|----------|
| `Xenova/bge-small-en-v1.5` | 384 | 33MB | English text, fast |
| `Xenova/all-MiniLM-L6-v2` | 384 | 23MB | English text, smallest |
| `Xenova/multilingual-e5-small` | 384 | 470MB | 100+ languages |

```typescript
// English
const en = new LocalModeEmbeddings({
  model: transformers.embedding('Xenova/bge-small-en-v1.5'),
});

// Multilingual
const multi = new LocalModeEmbeddings({
  model: transformers.embedding('Xenova/multilingual-e5-small'),
});
```

<Callout>
For the full list of embedding models, see the [Transformers Embeddings](/docs/transformers/embeddings) guide.
</Callout>

## Migration from OpenAI

```diff
- import { OpenAIEmbeddings } from '@langchain/openai';
+ import { LocalModeEmbeddings } from '@localmode/langchain';
+ import { transformers } from '@localmode/transformers';

- const embeddings = new OpenAIEmbeddings({ modelName: 'text-embedding-3-small' });
+ const embeddings = new LocalModeEmbeddings({
+   model: transformers.embedding('Xenova/bge-small-en-v1.5'),
+ });

// Usage is identical:
const vectors = await embeddings.embedDocuments(texts);
const queryVec = await embeddings.embedQuery('search');
```

## Composed Block

| Block | Description | Links |
|-----|-------------|-------|
| Semantic Search | Text/PDF/OCR ingest with chunking, then reranked vector search with category facets and calibrated thresholds — on a core ⇄ LangChain engine toggle | [Live](https://localmode.ai/blocks/knowledge/semantic-search) · Install: `npx shadcn add @localmode/ui/blocks/knowledge/semantic-search` |
