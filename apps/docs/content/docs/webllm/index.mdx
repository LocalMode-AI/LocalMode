---
title: 'Overview'
description: WebLLM provider for local LLM inference in the browser.
icon: Bot
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Accordions, Accordion } from 'fumadocs-ui/components/accordion';
import { TypeTable } from 'fumadocs-ui/components/type-table';

# @localmode/webllm

Run large language models locally in the browser using WebGPU. Uses 4-bit quantized models for efficient inference.

<Callout type="info" title="See it in action">
  Try [LLM Chat](https://localmode.ai/llm-chat) and [Research Agent](https://localmode.ai/research-agent) for working demos.
</Callout>

## Features

- 🚀 **WebGPU Acceleration** — Native GPU performance in the browser
- 🔒 **Private** — Models run entirely on-device
- 📦 **Cached** — Models stored in browser cache after first download
- ⚡ **Streaming** — Real-time token generation

## Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">```bash pnpm install @localmode/webllm @localmode/core ```</Tab>
  <Tab value="npm">```bash npm install @localmode/webllm @localmode/core ```</Tab>
  <Tab value="yarn">```bash yarn add @localmode/webllm @localmode/core ```</Tab>
  <Tab value="bun">```bash bun add @localmode/webllm @localmode/core ```</Tab>
</Tabs>

## Quick Start

```typescript
import { streamText } from '@localmode/core';
import { webllm } from '@localmode/webllm';

const model = webllm.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC');

const result = await streamText({
  model,
  prompt: 'Explain quantum computing in simple terms.',
});

let fullText = '';
for await (const chunk of result.stream) {
  fullText += chunk.text;
  // Update your UI with each chunk
}
```

## Available Models

<Accordions>

<Accordion title="Tiny Models (< 500MB)">

| Model                                  | Size   | Context | Best For                            |
| -------------------------------------- | ------ | ------- | ----------------------------------- |
| `SmolLM2-135M-Instruct-q0f16-MLC`   | ~78MB  | 2K      | Tiniest model, instant loading      |
| `SmolLM2-360M-Instruct-q4f16_1-MLC`   | ~210MB | 2K      | Ultra-fast, minimal memory          |
| `Qwen2.5-0.5B-Instruct-q4f16_1-MLC`   | ~278MB | 4K      | Tiny Qwen, great quality for size   |
| `Qwen3-0.6B-q4f16_1-MLC`              | ~350MB | 4K      | Qwen 3 0.6B, latest tiny model     |
| `TinyLlama-1.1B-Chat-v1.0-q4f16_1-MLC`| ~400MB | 2K      | TinyLlama 1.1B, fast and capable    |

<Callout type="info">
  Tiny models load in seconds and work well for simple tasks, autocomplete, and prototyping. Great
  for testing on low-end devices.
</Callout>

</Accordion>

<Accordion title="Small Models (500MB – 1GB)">

| Model                                  | Size   | Context | Best For                           |
| -------------------------------------- | ------ | ------- | ---------------------------------- |
| `Llama-3.2-1B-Instruct-q4f16_1-MLC`   | ~712MB | 4K      | Great for simple tasks             |
| `Qwen2.5-1.5B-Instruct-q4f16_1-MLC`   | ~868MB | 4K      | Multilingual, Chinese support      |
| `Qwen2.5-Coder-1.5B-Instruct-q4f16_1-MLC` | ~868MB | 4K  | Code-specialized, fast             |

<Callout type="tip">
  The Llama 3.2 1B is the best starting point for testing and development. Fast to download and
  provides good quality for its size. Qwen 2.5 Coder 1.5B is ideal for code generation tasks.
</Callout>

</Accordion>

<Accordion title="Medium Models (1 – 2GB)">

| Model                                  | Size    | Context | Best For                                     |
| -------------------------------------- | ------- | ------- | -------------------------------------------- |
| `Qwen3-1.7B-q4f16_1-MLC`              | ~1.1GB  | 4K      | Latest multilingual (recommended)            |
| `SmolLM2-1.7B-Instruct-q4f16_1-MLC`   | ~1GB    | 2K      | Best small model (requires `shader-f16`)     |
| `gemma-2-2b-it-q4f16_1-MLC`           | ~1.44GB | 2K      | Google Gemma 2 (requires `shader-f16`)       |
| `Qwen2.5-3B-Instruct-q4f16_1-MLC`     | ~1.7GB  | 4K      | High quality multilingual                    |
| `Qwen2.5-Coder-3B-Instruct-q4f16_1-MLC` | ~1.7GB | 4K     | Mid-range code model                         |
| `Llama-3.2-3B-Instruct-q4f16_1-MLC`   | ~1.76GB | 4K      | Excellent quality, general purpose           |
| `Hermes-3-Llama-3.2-3B-q4f16_1-MLC`   | ~1.76GB | 4K      | Enhanced chat, Hermes fine-tune              |
| `Ministral-3-3B-Instruct-2512-BF16-q4f16_1-MLC` | ~1.8GB | 4K | Mistral Ministral 3B, latest architecture |
| `Ministral-3-3B-Reasoning-2512-q4f16_1-MLC` | ~1.8GB | 4K | Ministral 3B, reasoning-tuned               |

<Callout type="tip">
  Medium models offer the best balance of quality and resource usage. Llama 3.2 3B and Ministral 3B
  are the top picks for production browser apps.
</Callout>

</Accordion>

<Accordion title="Large Models (> 2GB)">

| Model                                        | Size    | Context | Best For                          |
| -------------------------------------------- | ------- | ------- | --------------------------------- |
| `Phi-3.5-mini-instruct-q4f16_1-MLC`          | ~2.1GB  | 4K      | Excellent reasoning and coding     |
| `Phi-3-mini-4k-instruct-q4f16_1-MLC`         | ~2.2GB  | 4K      | Phi-3, reasoning and coding        |
| `Phi-3.5-vision-instruct-q4f16_1-MLC`       | ~2.4GB  | 1K      | Multimodal (text + images)         |
| `Qwen3-4B-q4f16_1-MLC`                      | ~2.2GB  | 4K      | Best quality in medium param range |
| `Mistral-7B-Instruct-v0.3-q4f16_1-MLC`      | ~4GB    | 4K      | Strong general-purpose            |
| `Qwen2.5-7B-Instruct-q4f16_1-MLC`           | ~4GB    | 4K      | Excellent multilingual            |
| `Qwen2.5-Coder-7B-Instruct-q4f16_1-MLC`     | ~4GB    | 4K      | Best-in-class code model          |
| `DeepSeek-R1-Distill-Qwen-7B-q4f16_1-MLC`   | ~4.18GB | 4K      | Advanced reasoning                |
| `DeepSeek-R1-Distill-Llama-8B-q4f16_1-MLC`  | ~4.41GB | 4K      | Best reasoning                    |
| `Hermes-3-Llama-3.1-8B-q4f16_1-MLC`         | ~4.9GB  | 4K      | Hermes 3 8B, DPO-optimized chat   |
| `Llama-3.1-8B-Instruct-q4f16_1-MLC`         | ~4.5GB  | 4K      | Strong general-purpose            |
| `Qwen3-8B-q4f16_1-MLC`                      | ~4.5GB  | 4K      | Highest quality multilingual      |
| `gemma-2-9b-it-q4f16_1-MLC`                 | ~5GB    | 1K      | Google Gemma 2 9B, highest quality |

<Callout type="warn">
  Large models require significant GPU memory (4GB+ VRAM). Best for desktop browsers with dedicated
  GPUs. Not recommended for mobile devices.
</Callout>

</Accordion>

</Accordions>

<Callout type="info" title="Model Selection Guide">
  - **Testing**: `Llama-3.2-1B-Instruct` — fastest to download and run
  - **Production**: `Llama-3.2-3B-Instruct` or `Phi-3.5-mini` — best quality-to-size ratio
  - **Code**: `Qwen2.5-Coder-1.5B` (fast) or `Qwen2.5-Coder-7B` (best quality)
  - **Reasoning**: `Ministral-3-3B-Reasoning` (compact) or `DeepSeek-R1-Distill-Qwen-7B` (best)
  - **Multilingual**: `Qwen3-1.7B` or `Qwen3-8B` — latest generation, 100+ languages
  - **Multimodal**: `Phi-3.5-vision` — text + image understanding
  - **General 7B+**: `Mistral-7B-v0.3`, `Llama-3.1-8B`, `Qwen2.5-7B`, or `gemma-2-9b` — desktop-class quality
  - **Low Memory**: `SmolLM2-135M-Instruct` — ~78MB, instant loading
</Callout>

## Text Generation

### Streaming

```typescript
import { streamText } from '@localmode/core';

const result = await streamText({
  model: webllm.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC'),
  prompt: 'Write a haiku about programming.',
});

let fullText = '';
for await (const chunk of result.stream) {
  fullText += chunk.text;
  // Update UI with each chunk
}

// Or get full text at once
const text = await result.text;
```

### Non-Streaming

```typescript
import { generateText } from '@localmode/core';

const { text, usage } = await generateText({
  model: webllm.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC'),
  prompt: 'What is the capital of France?',
});

console.log(text);
console.log('Tokens used:', usage.totalTokens);
```

## Configuration

### Model Options

```ts {2-6}
const model = webllm.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC', {
  systemPrompt: 'You are a helpful coding assistant.',
  temperature: 0.7,
  maxTokens: 1024,
  topP: 0.9,
});
```

<TypeTable
  type={{
    systemPrompt: {
      description: 'System message prepended to all conversations',
      type: 'string',
    },
    temperature: {
      description: 'Randomness in generation (0.0 = deterministic, 2.0 = very random)',
      type: 'number',
      default: '0.7',
    },
    maxTokens: {
      description: 'Maximum tokens to generate',
      type: 'number',
      default: '512',
    },
    topP: {
      description: 'Nucleus sampling (0.0-1.0)',
      type: 'number',
      default: '0.95',
    },
  }}
/>

### Custom Provider

```typescript
import { createWebLLM } from '@localmode/webllm';

const myWebLLM = createWebLLM({
  useWorker: true, // Run inference in a Web Worker (default: true)
  onProgress: (progress) => {
    console.log(`Loading: ${(progress.progress * 100).toFixed(1)}%`);
    console.log(`Status: ${progress.text}`);
  },
});

const model = myWebLLM.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC');
```

## Model Preloading

Preload models during app initialization:

```typescript
import { preloadModel, isModelCached } from '@localmode/webllm';

// Check if already cached
if (!(await isModelCached('Llama-3.2-1B-Instruct-q4f16_1-MLC'))) {
  // Show loading UI
  await preloadModel('Llama-3.2-1B-Instruct-q4f16_1-MLC', {
    onProgress: (progress) => {
      updateLoadingBar(progress.progress * 100);
    },
  });
}

// Model is ready for instant inference
```

## Model Management

### Available Models Registry

Access model metadata programmatically:

```typescript
import { WEBLLM_MODELS, type WebLLMModelId } from '@localmode/webllm';

// Get all available models
const modelIds = Object.keys(WEBLLM_MODELS) as WebLLMModelId[];

// Access model info
const llama = WEBLLM_MODELS['Llama-3.2-1B-Instruct-q4f16_1-MLC'];
console.log(llama.name);          // 'Llama 3.2 1B Instruct'
console.log(llama.contextLength); // 4096
console.log(llama.size);          // '~700MB'
console.log(llama.sizeBytes);     // 734003200
console.log(llama.description);   // 'Fast, lightweight model...'
```

### Model Categorization

Categorize models by size for UI display:

```typescript
import { getModelCategory, WEBLLM_MODELS, type WebLLMModelId } from '@localmode/webllm';

// Get category based on model size
const modelId: WebLLMModelId = 'Llama-3.2-1B-Instruct-q4f16_1-MLC';
const sizeBytes = WEBLLM_MODELS[modelId].sizeBytes;
const category = getModelCategory(sizeBytes);

console.log(category); // 'tiny' | 'small' | 'medium' | 'large'

// Use for UI grouping
function getModelsByCategory() {
  const categories = { tiny: [], small: [], medium: [], large: [] };
  
  for (const [id, info] of Object.entries(WEBLLM_MODELS)) {
    const cat = getModelCategory(info.sizeBytes);
    categories[cat].push({ id, ...info });
  }
  
  return categories;
}
```

### Delete Cached Models

Remove models from browser cache to free up storage:

```typescript
import { deleteModelCache, isModelCached } from '@localmode/webllm';

// Delete a specific model's cache
await deleteModelCache('Llama-3.2-1B-Instruct-q4f16_1-MLC');

// Verify deletion
const stillCached = await isModelCached('Llama-3.2-1B-Instruct-q4f16_1-MLC');
console.log(stillCached); // false
```

<Callout type="tip" title="Storage Management">
  LLM models can be large (700MB - 4GB). Use `deleteModelCache()` to let users
  free up storage when they no longer need a model.
</Callout>

### Type-Safe Model IDs

Use the `WebLLMModelId` type for type-safe model selection:

```typescript
import type { WebLLMModelId } from '@localmode/webllm';

// Type-safe function that only accepts valid model IDs
function selectModel(modelId: WebLLMModelId) {
  return webllm.languageModel(modelId);
}

// ✅ Valid
selectModel('Llama-3.2-1B-Instruct-q4f16_1-MLC');

// ❌ TypeScript error: invalid model ID
selectModel('invalid-model-name');
```

## Chat Application

```typescript
import { streamText } from '@localmode/core';

interface Message {
  role: 'user' | 'assistant';
  content: string;
}

async function chat(messages: Message[], userMessage: string) {
  const model = webllm.languageModel('Llama-3.2-1B-Instruct-q4f16_1-MLC', {
    systemPrompt: 'You are a helpful assistant.',
  });

  // Build conversation prompt
  const prompt = messages
    .map((m) => `${m.role}: ${m.content}`)
    .concat([`user: ${userMessage}`, 'assistant:'])
    .join('\n');

  const result = await streamText({
    model,
    prompt,
    stopSequences: ['user:', '\n\n'],
  });

  let response = '';
  for await (const chunk of result.stream) {
    response += chunk.text;
    // Update UI
  }

  return response;
}
```

## RAG Integration

Combine with retrieval for document-grounded chat:

```typescript
import { semanticSearch, rerank, streamText } from '@localmode/core';

async function ragChat(query: string, db: VectorDB) {
  // 1. Retrieve context
  const results = await semanticSearch({
    db,
    model: embeddingModel,
    query,
    k: 10,
  });

  // 2. Rerank for relevance
  const reranked = await rerank({
    model: rerankerModel,
    query,
    documents: results.map((r) => r.metadata.text as string),
    topK: 3,
  });

  const context = reranked.results.map((r) => r.text).join('\n\n---\n\n');

  // 3. Generate with context
  const llm = webllm.languageModel('Llama-3.2-3B-Instruct-q4f16_1-MLC');

  const result = await streamText({
    model: llm,
    prompt: `You are a helpful assistant. Answer based only on the provided context.
If the answer is not in the context, say "I don't have that information."

Context:
${context}

Question: ${query}

Answer:`,
  });

  return result;
}
```

## Requirements

<Callout type="warn" title="WebGPU Required">
  WebLLM requires WebGPU support. Check availability:
  
  ```typescript
  import { isWebGPUSupported } from '@localmode/core';
  
  if (!isWebGPUSupported()) {
    console.warn('WebGPU not available. LLM features disabled.');
  }
  ```
</Callout>

### Browser Support

| Browser     | Support           |
| ----------- | ----------------- |
| Chrome 113+ | ✅                |
| Edge 113+   | ✅                |
| Firefox     | ❌ (Nightly only) |
| Safari 18+  | ✅                |
| iOS Safari  | ✅ (iOS 26+)      |

### Hardware Requirements

- **GPU**: Any modern GPU with WebGPU support
- **VRAM**: Depends on model (1-3GB for 1-3B models)
- **RAM**: 4GB minimum, 8GB+ recommended

## Vision (Image Input)

WebLLM supports vision input via **Phi 3.5 Vision** (2.4GB). Send images as `ImagePart` content alongside text.

```typescript
import { streamText } from '@localmode/core';
import { webllm } from '@localmode/webllm';

const model = webllm.languageModel('Phi-3.5-vision-instruct-q4f16_1-MLC');

// Check vision support
console.log(model.supportsVision); // true

// Send image with text
const result = await streamText({
  model,
  prompt: '',
  messages: [{
    role: 'user',
    content: [
      { type: 'text', text: 'What do you see in this photo?' },
      { type: 'image', data: base64Data, mimeType: 'image/jpeg' },
    ],
  }],
});
```

Images are converted internally to OpenAI-compatible `image_url` format with data URIs. Accepted formats: JPEG, PNG, WebP, GIF.

<Callout type="info">
  For full multimodal API reference including `ContentPart` types and utilities, see the [Core Generation](/docs/core/generation#vision-multimodal-input) guide.
</Callout>

## Best Practices

<Callout type="tip" title="WebLLM Tips">
  1. **Preload models** - Load during app init for instant inference 
  2. **Start small** - Use 1B models for testing, larger for production 
  3. **Stream responses** - Better UX than waiting for complete response 
  4. **Handle errors** - GPU errors, OOM, etc. can occur 
  5. **Check capabilities** - Verify WebGPU before showing LLM features
</Callout>

## Error Handling

```typescript
import { streamText, GenerationError } from '@localmode/core';

try {
  const result = await streamText({
    model,
    prompt: 'Hello',
  });

  for await (const chunk of result.stream) {
    // ...
  }
} catch (error) {
  if (error instanceof GenerationError) {
    if (error.code === 'WEBGPU_NOT_SUPPORTED') {
      console.error('WebGPU not available');
    } else if (error.code === 'MODEL_LOAD_FAILED') {
      console.error('Failed to load model');
    } else if (error.code === 'OUT_OF_MEMORY') {
      console.error('Not enough GPU memory');
    }
  }
}
```

## Next Steps

<Cards>
  <Card
    title="Text Generation"
    href="/docs/core/generation"
    description="Learn about streaming and generation options."
  />
  <Card
    title="RAG"
    href="/docs/core/rag"
    description="Build retrieval-augmented generation pipelines."
  />
  <Card
    title="Capabilities"
    href="/docs/core/capabilities"
    description="Detect WebGPU and other features."
  />
</Cards>

## Showcase Apps

| App | Description | Links |
|-----|-------------|-------|
| LLM Chat | Multi-model chat with WebGPU-accelerated LLM inference and vision (Phi 3.5 Vision) | [Demo](https://localmode.ai/llm-chat) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/showcase-nextjs/src/app/(apps)/llm-chat) |
| Data Extractor | Structured data extraction using WebLLM models | [Demo](https://localmode.ai/data-extractor) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/showcase-nextjs/src/app/(apps)/data-extractor) |
| Research Agent | ReAct agent powered by WebLLM language models | [Demo](https://localmode.ai/research-agent) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/showcase-nextjs/src/app/(apps)/research-agent) |
| LangChain RAG | RAG pipeline with WebLLM for answer generation | [Demo](https://localmode.ai/langchain-rag) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/showcase-nextjs/src/app/(apps)/langchain-rag) |
| PDF Search | PDF Q&A with WebLLM-powered response generation | [Demo](https://localmode.ai/pdf-search) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/showcase-nextjs/src/app/(apps)/pdf-search) |
