---
title: Middleware
description: Extend functionality with caching, logging, retry, and validation.
icon: Layers
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Accordions, Accordion } from 'fumadocs-ui/components/accordion';
import { TypeTable } from 'fumadocs-ui/components/type-table';


Middleware lets you extend and modify the behavior of embedding models and vector databases. There are two kinds, and each built-in middleware belongs to exactly one of them:

| Kind | Wrap with | Built-in middleware |
| --- | --- | --- |
| Embedding model middleware | `wrapEmbeddingModel({ model, middleware })` | `retryMiddleware`, `rateLimitMiddleware`, `piiRedactionMiddleware`, `dpEmbeddingMiddleware` |
| VectorDB middleware | `wrapVectorDB({ db, middleware })` | `cachingMiddleware`, `loggingMiddleware`, `validationMiddleware`, `encryptionMiddleware` |

<Callout type="info" title="See it in action">
  Try the [PII Redactor block](https://localmode.ai/blocks/privacy/pii-redactor) for a working demo of these APIs.
</Callout>

## Embedding Model Middleware

Wrap an embedding model with a single middleware object. To stack several, combine them with `composeEmbeddingMiddleware()` (see [Middleware Composition](#middleware-composition)):

```ts
import { wrapEmbeddingModel, retryMiddleware } from '@localmode/core';
import { transformers } from '@localmode/transformers';

const baseModel = transformers.embedding('Xenova/bge-small-en-v1.5');

const model = wrapEmbeddingModel({
  model: baseModel,
  middleware: retryMiddleware({ maxRetries: 3 }),
});
```

<Accordions>

<Accordion title="Retry Middleware">

Retry failed embedding calls with exponential backoff:

```ts
import { wrapEmbeddingModel, retryMiddleware, RetryPredicates } from '@localmode/core';

const model = wrapEmbeddingModel({
  model: baseModel,
  middleware: retryMiddleware({
    maxRetries: 3,
    initialDelayMs: 100,
    maxDelayMs: 5000,
    backoffMultiplier: 2,
    shouldRetry: RetryPredicates.networkErrors, // or (error, attempt) => boolean
  }),
});
```

<TypeTable
  type={{
    maxRetries: {
      description: 'Maximum retry attempts',
      type: 'number',
      default: '3',
    },
    initialDelayMs: {
      description: 'Delay before the first retry, in milliseconds',
      type: 'number',
      default: '100',
    },
    maxDelayMs: {
      description: 'Upper bound on the delay between retries, in milliseconds',
      type: 'number',
      default: '5000',
    },
    backoffMultiplier: {
      description: 'Factor the delay grows by after each attempt',
      type: 'number',
      default: '2',
    },
    jitter: {
      description: 'Randomize each delay by ±25%',
      type: 'boolean',
      default: 'true',
    },
    shouldRetry: {
      description: 'Return true to retry the failed attempt (default: retry every error)',
      type: '(error: Error, attempt: number) => boolean',
    },
  }}
/>

`RetryPredicates` provides ready-made `shouldRetry` functions: `networkErrors`, `rateLimitErrors`, `serverErrors`, `always`, and `never`.

</Accordion>

<Accordion title="Rate Limit Middleware">

Limit how many embedding calls run per time window. Calls over the limit wait until the window frees up:

```ts
import { wrapEmbeddingModel, rateLimitMiddleware } from '@localmode/core';

const model = wrapEmbeddingModel({
  model: baseModel,
  middleware: rateLimitMiddleware({
    maxRequests: 10, // Max requests
    windowMs: 1000, // Per second
    onRateLimit: (waitMs) => {
      console.warn(`Rate limit hit, waiting ${waitMs}ms`);
    },
  }),
});
```

<TypeTable
  type={{
    maxRequests: {
      description: 'Maximum requests per window',
      type: 'number',
      default: '100',
    },
    windowMs: {
      description: 'Window size in milliseconds',
      type: 'number',
      default: '1000',
    },
    queue: {
      description: 'Queue over-limit calls (processed in order) instead of having each wait independently',
      type: 'boolean',
      default: 'false',
    },
    maxQueueSize: {
      description: 'Maximum queued calls; a call beyond this throws',
      type: 'number',
      default: '1000',
    },
    onRateLimit: {
      description: 'Called with the wait time whenever the limit is hit',
      type: '(waitMs: number) => void',
    },
  }}
/>

</Accordion>

<Accordion title="PII Redaction Middleware">

Redact sensitive information before embedding:

```ts
import { embed, wrapEmbeddingModel, piiRedactionMiddleware } from '@localmode/core';

const model = wrapEmbeddingModel({
  model: baseModel,
  middleware: piiRedactionMiddleware({ emails: true, phones: true }),
});

// Emails, phone numbers, etc. are redacted before embedding
const { embedding } = await embed({
  model,
  value: 'Contact john@example.com or call 555-123-4567',
});
// Actually embeds: "Contact [EMAIL_REDACTED] or call [PHONE_REDACTED]"
```

<TypeTable
  type={{
    emails: { description: 'Redact email addresses', type: 'boolean', default: 'true' },
    phones: { description: 'Redact phone numbers', type: 'boolean', default: 'true' },
    ssn: { description: 'Redact SSN patterns', type: 'boolean', default: 'true' },
    creditCards: { description: 'Redact credit card numbers', type: 'boolean', default: 'true' },
    ipAddresses: { description: 'Redact IP addresses', type: 'boolean', default: 'false' },
    dates: { description: 'Redact dates', type: 'boolean', default: 'false' },
    customPatterns: {
      description: 'Extra patterns to redact',
      type: 'Array<{ pattern: RegExp; replacement: string }>',
    },
    replacement: {
      description: 'One replacement for every category (default: a per-category tag such as [EMAIL_REDACTED])',
      type: 'string',
    },
    replacements: {
      description: 'Per-category replacements, used when replacement is not set',
      type: '{ email?, phone?, ssn?, creditCard?, ipAddress?, date?: string }',
    },
  }}
/>

</Accordion>

</Accordions>

For differentially private embeddings, see `dpEmbeddingMiddleware` in [Differential Privacy](/docs/core/differential-privacy).

## Vector DB Middleware

Wrap a vector database with one middleware object or an array of them. The wrapped database has the same `VectorDB` interface as the original:

```typescript
import { createVectorDB, wrapVectorDB } from '@localmode/core';

const baseDB = await createVectorDB({ name: 'db', dimensions: 384 });

const db = wrapVectorDB({
  db: baseDB,
  middleware: {
    beforeAdd: async (doc) => {
      console.log('Adding', doc.id);
      return doc;
    },
    beforeSearch: async (query, options) => {
      console.log('Searching with k =', options.k);
      return { query, options };
    },
    afterSearch: async (results) => {
      console.log('Found', results.length, 'results');
      return results;
    },
    beforeDelete: async (id) => {
      console.log('Deleting', id);
      return true; // return false to cancel the delete
    },
  },
});
```

<Accordions>

<Accordion title="Caching Middleware">

Answer repeated `search()` and `get()` calls from an in-memory LRU cache. Any write through the wrapped database (`add`, `addMany`, `update`, `delete`, `deleteMany`, `deleteWhere`, `import`, `clear`) invalidates the cache, so results never go stale:

```ts
import { wrapVectorDB, cachingMiddleware } from '@localmode/core';

const db = wrapVectorDB({
  db: baseDB,
  middleware: cachingMiddleware({
    maxSearchResults: 100, // Maximum cached entries
    ttlMs: 60_000, // Time-to-live: 1 minute
  }),
});

// First search runs against the index
const r1 = await db.search(queryVector, { k: 5 });

// The same search again is answered from the cache
const r2 = await db.search(queryVector, { k: 5 });
```

<TypeTable
  type={{
    maxSearchResults: {
      description: 'Maximum cached entries (applies to the search cache and the document cache separately)',
      type: 'number',
      default: '100',
    },
    ttlMs: {
      description: 'Time-to-live in milliseconds',
      type: 'number',
      default: '60000',
    },
    cacheSearchResults: {
      description: 'Cache search() results',
      type: 'boolean',
      default: 'true',
    },
    cacheDocuments: {
      description: 'Cache get() results',
      type: 'boolean',
      default: 'true',
    },
  }}
/>

The search cache key covers the full query vector plus `k`, `filter`, `threshold`, and `includeVectors`, so different searches never share an entry. The cache only sees writes made through the wrapped database; writes made directly on the unwrapped instance are not observed.

<Callout type="info">
  To cache embeddings or LLM responses instead of vector searches, see [Semantic Cache](/docs/core/semantic-cache).
</Callout>

</Accordion>

<Accordion title="Logging Middleware">

Log database operations:

```ts
import { wrapVectorDB, loggingMiddleware } from '@localmode/core';

const db = wrapVectorDB({
  db: baseDB,
  middleware: loggingMiddleware({
    level: 'info',
    operations: ['add', 'search'],
    timing: true,
  }),
});
// [VectorDB:INFO] search:start { k: 5, hasFilter: false, dimensions: 384 }
// [VectorDB:INFO] search:complete { resultCount: 5, topScore: 0.91, durationMs: 1.2 }
```

<TypeTable
  type={{
    logger: {
      description: 'Log function',
      type: '(...args: unknown[]) => void',
      default: 'console.log',
    },
    level: {
      description: 'Level shown in the log prefix',
      type: "'debug' | 'info' | 'warn' | 'error'",
      default: "'info'",
    },
    timing: {
      description: 'Include durationMs in completion entries',
      type: 'boolean',
      default: 'true',
    },
    operations: {
      description: 'Operations to log',
      type: "Array<'add' | 'get' | 'delete' | 'search' | 'clear'>",
      default: 'all',
    },
    formatter: {
      description: 'Format each entry as a single string passed to logger',
      type: '(operation: string, data: Record<string, unknown>) => string',
    },
  }}
/>

</Accordion>

<Accordion title="Validation Middleware">

Reject malformed documents before they are stored:

```ts
import { wrapVectorDB, validationMiddleware } from '@localmode/core';

const db = wrapVectorDB({
  db: baseDB,
  middleware: validationMiddleware({
    dimensions: 384,
    validateValues: true, // Reject NaN / Infinity
    customValidator: (doc) => (doc.metadata?.source ? true : 'metadata.source is required'),
  }),
});
```

<TypeTable
  type={{
    dimensions: {
      description: 'Required vector length',
      type: 'number',
    },
    validateValues: {
      description: 'Reject NaN and Infinity vector values',
      type: 'boolean',
      default: 'true',
    },
    validateMetadata: {
      description: 'Enforce maxMetadataSize',
      type: 'boolean',
      default: 'true',
    },
    maxMetadataSize: {
      description: 'Maximum serialized metadata size in bytes',
      type: 'number',
      default: '1048576 (1MB)',
    },
    maxTextLength: {
      description: 'Maximum length of the __text metadata field',
      type: 'number',
      default: '102400 (100KB)',
    },
    customValidator: {
      description: 'Return true to accept, or a string error message to reject',
      type: '(document: Document) => boolean | string',
    },
  }}
/>

</Accordion>

<Accordion title="Encryption Middleware">

Encrypt document metadata before it reaches storage; it is decrypted again on `get()` and in search results:

```ts
import { createVectorDB, wrapVectorDB, encryptionMiddleware, deriveEncryptionKey } from '@localmode/core';

const { key } = await deriveEncryptionKey('user-password');

const db = wrapVectorDB({
  db: await createVectorDB({ name: 'secure', dimensions: 384 }),
  middleware: encryptionMiddleware({
    key,
    excludeFields: ['category'], // keep filterable fields in plaintext
  }),
});
```

<Callout type="warn">
  Vectors stay in plaintext: the index computes distances over them, so it can neither store nor
  search ciphertext. `encryptVectors` must be omitted or `false`; passing `true` throws a
  `ValidationError`. To protect vectors at rest, encrypt an exported snapshot with `encryptVector()`.
  Store the key securely: if it is lost, encrypted metadata cannot be decrypted.
</Callout>

See [Security](/docs/core/security) for key derivation and the full option list.

</Accordion>

</Accordions>

### Vector DB Middleware Interface

Every hook is optional:

```typescript
interface VectorDBMiddleware {
  beforeAdd?: (document: Document) => Document | Promise<Document>;
  afterAdd?: (document: Document) => void | Promise<void>;
  wrapGet?: (options: {
    doGet: () => Promise<Document | null>;
    id: string;
  }) => Promise<Document | null>;
  afterGet?: (document: Document | undefined) => Document | undefined | Promise<Document | undefined>;
  beforeDelete?: (id: string) => boolean | Promise<boolean>; // false cancels
  afterDelete?: (id: string) => void | Promise<void>;
  beforeSearch?: (
    query: Float32Array,
    options: SearchOptions
  ) => { query: Float32Array; options: SearchOptions } | Promise<{ query: Float32Array; options: SearchOptions }>;
  wrapSearch?: (options: {
    doSearch: () => Promise<SearchResult[]>;
    query: Float32Array;
    options: SearchOptions;
  }) => Promise<SearchResult[]>;
  afterSearch?: (results: SearchResult[]) => SearchResult[] | Promise<SearchResult[]>;
  afterUpdate?: (id: string) => void | Promise<void>;
  afterDeleteWhere?: (deletedCount: number) => void | Promise<void>;
  afterImport?: () => void | Promise<void>;
  beforeClear?: () => boolean | Promise<boolean>; // false cancels
  afterClear?: () => void | Promise<void>;
  onError?: (error: Error, operation: string) => boolean | void | Promise<boolean | void>;
}
```

`wrapSearch` and `wrapGet` wrap the operation itself: call `doSearch()` / `doGet()` to run it (through any inner middleware), or return a value without calling it to answer from elsewhere, such as a cache. For a search the order is `beforeSearch` → `wrapSearch` → `afterSearch`; for a get it is `wrapGet` → `afterGet`. `afterUpdate`, `afterDeleteWhere`, and `afterImport` fire after `update()`, `deleteWhere()`, and `import()` complete, which is where a middleware that keeps derived state (like the caching middleware) invalidates it.

### Error Handling

`onError` receives every error thrown by a wrapped operation (`add`, `addMany`, `get`, `update`, `delete`, `deleteMany`, `deleteWhere`, `search`, `clear`, `import`) together with the operation name. Return `true` to suppress the error: the operation then resolves with a neutral value instead of rejecting. Return `false` or nothing to let the original error propagate.

| Operation | Resolves with when suppressed |
| --- | --- |
| `get` | `null` |
| `search` | `[]` |
| `deleteWhere` | `0` |
| `add`, `addMany`, `update`, `delete`, `deleteMany`, `clear`, `import` | `undefined` |

```typescript
const db = wrapVectorDB({
  db: baseDB,
  middleware: {
    onError: (error, operation) => {
      console.error(`VectorDB ${operation} failed:`, error);
      return operation === 'search'; // a failed search yields [], every other failure still throws
    },
  },
});
```

With several middleware, the first one whose `onError` returns `true` suppresses the error. The built-in `loggingMiddleware` logs errors and never suppresses them.

## Middleware Composition

Combine multiple middleware into a single middleware using composition functions:

```typescript
import { composeEmbeddingMiddleware, composeVectorDBMiddleware } from '@localmode/core';
```

### Composing Embedding Middleware

```typescript
import {
  composeEmbeddingMiddleware,
  piiRedactionMiddleware,
  retryMiddleware,
  wrapEmbeddingModel,
} from '@localmode/core';

const combined = composeEmbeddingMiddleware([
  piiRedactionMiddleware({ emails: true, phones: true }),
  retryMiddleware({ maxRetries: 3 }),
]);

const secureModel = wrapEmbeddingModel({ model: baseModel, middleware: combined });
```

### Composing VectorDB Middleware

`wrapVectorDB` accepts an array directly (it composes it for you), or you can compose explicitly with `composeVectorDBMiddleware`:

```typescript
import {
  composeVectorDBMiddleware,
  cachingMiddleware,
  loggingMiddleware,
  validationMiddleware,
  wrapVectorDB,
} from '@localmode/core';

const combined = composeVectorDBMiddleware([
  validationMiddleware({ dimensions: 384 }),
  loggingMiddleware({ operations: ['search'] }),
  cachingMiddleware({ maxSearchResults: 500 }),
]);

const wrappedDB = wrapVectorDB({ db, middleware: combined });
```

<Callout type="info" title="Middleware Order">
  `before*` and `after*` hooks run in array order. `wrapSearch` / `wrapGet` nest with the first
  middleware outermost, so in the example above the logger records every search, including the ones
  the cache answers. Put the logger after the cache to log only searches that reach the index.
</Callout>

## Factory Aliases

Each built-in middleware also has a `create*` alias with the same signature and behavior:

| Factory | Same as | Options Type |
| --- | --- | --- |
| `createCachingMiddleware(options)` | `cachingMiddleware` | `CachingMiddlewareOptions` |
| `createLoggingMiddleware(options)` | `loggingMiddleware` | `LoggingMiddlewareOptions` |
| `createValidationMiddleware(options)` | `validationMiddleware` | `ValidationMiddlewareOptions` |
| `createRetryMiddleware(options)` | `retryMiddleware` | `RetryMiddlewareOptions` |
| `createRateLimitMiddleware(options)` | `rateLimitMiddleware` | `RateLimitMiddlewareOptions` |

## Custom Middleware

### Custom Embedding Middleware

```typescript
import { wrapEmbeddingModel, type EmbeddingModelMiddleware } from '@localmode/core';

function timingMiddleware(): EmbeddingModelMiddleware {
  return {
    transformParams: async ({ values }) => {
      // Transform input values
      return { values: values.map((v) => v.trim()) };
    },
    wrapEmbed: async ({ doEmbed, values, model }) => {
      const start = Date.now();

      // Call the actual embedding function
      const result = await doEmbed();

      console.log(`Embedded ${values.length} values with ${model.modelId} in ${Date.now() - start}ms`);
      return result;
    },
  };
}

const model = wrapEmbeddingModel({ model: baseModel, middleware: timingMiddleware() });
```

### Custom VectorDB Middleware

Implement the `VectorDBMiddleware` interface to create custom middleware:

```typescript
import type { VectorDBMiddleware } from '@localmode/core';

function analyticsMiddleware(): VectorDBMiddleware {
  let searchCount = 0;
  let addCount = 0;

  return {
    wrapSearch: async ({ doSearch, options }) => {
      searchCount++;
      const start = performance.now();
      const results = await doSearch();
      console.log(
        `Search #${searchCount} (k=${options.k ?? 10}): ${results.length} results in ${performance.now() - start}ms`
      );
      return results;
    },

    afterAdd: async () => {
      addCount++;
      console.log(`Total documents added: ${addCount}`);
    },
  };
}
```

## Best Practices

<Callout type="tip" title="Middleware Tips">
  1. **Use the right wrapper** - Embedding middleware goes to `wrapEmbeddingModel`, VectorDB middleware to `wrapVectorDB`
  2. **Order matters** - Validation first; decide whether logging sits outside or inside the cache
  3. **Keep middleware focused** - One concern per middleware
  4. **Write through the wrapper** - Middleware only sees operations made on the wrapped instance
  5. **Consider performance** - Each middleware adds overhead
</Callout>

## Next Steps

<Cards>
  <Card
    title="Security"
    href="/docs/core/security"
    description="Learn about encryption and PII redaction."
  />
  <Card
    title="Embeddings"
    href="/docs/core/embeddings"
    description="Use middleware with embedding models."
  />
</Cards>

## Blocks

| App | Description | Links |
|-----|-------------|-------|
| Privacy (PII Redactor) | Differential privacy middleware for embedding models | [Live block](https://localmode.ai/blocks/privacy/pii-redactor) · [Source](https://github.com/LocalMode-AI/LocalMode/tree/main/apps/ui/src/app/blocks/privacy/pii-redactor) |
