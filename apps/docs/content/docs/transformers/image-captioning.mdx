---
title: Image Captioning
description: Generate text descriptions of images with Florence-2.
icon: Camera
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';

Generate natural language descriptions of images using vision-language models like Florence-2.

<Callout type="info">
  For full API reference (`captionImage()`, options, result types, and custom providers), see the [Core Vision](/docs/core/vision) guide.
</Callout>

<Callout type="info" title="See it in action">
  Run it in the [Image Captioner block](https://localmode.ai/blocks/image-studio/image-captioner) — an
  installable, registry-composed version with an accumulating gallery.
</Callout>

## Recommended Models

| Model | Size | Speed | Use Case |
|-------|------|-------|----------|
| `onnx-community/Florence-2-base-ft` | ~460MB | ⚡⚡ | Captioning, OCR, detection, and document QA |

## File Upload Example

Based on the [Image Captioner block](https://localmode.ai/blocks/image-studio/image-captioner):

```typescript
import { transformers } from '@localmode/transformers';
import { captionImage } from '@localmode/core';

const model = transformers.captioner('onnx-community/Florence-2-base-ft');

async function handleImageUpload(file: File) {
  const dataUrl = await new Promise<string>((resolve) => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result as string);
    reader.readAsDataURL(file);
  });

  const { caption } = await captionImage({
    model,
    image: dataUrl,
    abortSignal: controller.signal,
  });

  return caption;
}
```

## Image Input Formats

The `image` parameter accepts:
- `string` — Data URL (`data:image/jpeg;base64,...`) or regular URL
- `Blob` — Image blob from file input or fetch

## Best Practices

<Callout type="tip" title="Captioning Tips">
  1. **Use JPEG/PNG/WebP** — These formats are well-supported
  2. **Resize large images** — Smaller images process faster with similar quality
  3. **Cache the model** — Load once, caption many images
  4. **Handle errors** — Invalid or corrupted images will throw
</Callout>

## Composed Block

| Block | Description | Links |
|-----|-------------|-------|
| Image Captioner | ViT-GPT2 alt-text captioning with an accumulating gallery | [Live](https://localmode.ai/blocks/image-studio/image-captioner) · Install: `npx shadcn add @localmode/ui/blocks/image-studio/image-captioner` |

## Next Steps

<Cards>
  <Card
    title="Core Vision API"
    href="/docs/core/vision"
    description="Full API reference including captionImage()."
  />
  <Card
    title="Object Detection"
    href="/docs/transformers/object-detection"
    description="Detect objects with bounding boxes."
  />
  <Card
    title="Image Features"
    href="/docs/transformers/image-features"
    description="Extract feature vectors for image search."
  />
</Cards>
